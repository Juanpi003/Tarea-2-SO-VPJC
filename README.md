# Tarea-2-SO-VPJC
Módulo mini meminfo

Es un módulo que crea la entrada /proc/mem_simple. Al leerla con cat /proc/mem_simple, el módulo consulta en tiempo real la memoria del sistema y muestra un resumen con la memoria total, libre y usada, en kB y MB. 
